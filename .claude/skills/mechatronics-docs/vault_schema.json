{
  "schema_version": "0.3",
  "decision": "DECISIONS.md, amendments 2026-07-28b, 2026-07-28d and 2026-07-31b",
  "purpose": [
    "Declare, as data, which fields an object of a given domain carries and",
    "which named relations exist between objects. validate_vault.py READS this",
    "file: the field vocabulary, the permitted values and the required keys are",
    "taken from here, not from Python literals. Adding a field is an edit to",
    "this file, not a code change - which is what issue #4 asked for.",
    "Every entry carries an 'enforced' flag, and the flag is the switch, not a",
    "comment: see enforcement_levels.",
    "Since amendment 2026-07-31b this file has TWO readers. validate_vault.py",
    "reads the field vocabulary and the value lists; export_traceability.py",
    "reads the relations, the table bindings and the domain aliases. An entry",
    "flagged 'export-driven' is read by the exporter and by nothing else: it",
    "shapes a report, never a finding that blocks a turn."
  ],

  "enforcement_levels": {
    "schema-driven": "The validator reads this entry from this file. Changing the entry changes what is enforced, with no code change.",
    "validator-internal": "The rule lives in validate_vault.py and this file only describes it. Every occurrence names why it was not moved.",
    "export-driven": "export_traceability.py reads this entry. It shapes the exported graph and the report, and it never produces a validator finding - the exporter reports, it does not block.",
    "declared-only": "Nothing enforces this. It is declared so that readers - and the undeclared-field check - know the name exists."
  },

  "domain_aliases": {
    "note": "A vault written in another language spells its domain folders differently, and every role in this file is written with the English token. This map is the translation, and it is the reason the exporter finds a graph in a German vault at all: without it, 'the requirements domain' resolves to nothing, which is exactly what Vault.req_index() does today when it asks for the folder abbreviation REQ.",
    "decision": "Follow-up 4 of amendment 2026-07-28, unbundled from the convention rollout that blocked it there. It was deliberately not shipped with the validator because requiring 'verifies' in a German evidence domain would have raised 38 ERRORs on a vault that never had the convention. The exporter has no such problem: it reports and never blocks.",
    "map": {
      "ANF": "REQ",
      "ENT": "DEC",
      "KMP": "CMP",
      "SST": "IFC",
      "TUE": "TAE",
      "BUN": "OAU"
    },
    "not_an_alias": "TST appears in six homelab and two verdantia allocation-table headers as 'Verifikation (TST)', but no vault has ever had a TST folder - it is a stale column label, not a domain. It is deliberately absent here so the map cannot be read as evidence that such a domain exists.",
    "identity": ["REQ", "DEC", "ARC", "CMP", "IFC", "IMP", "TAE", "OAU", "REF"],
    "requirement_id_prefix": {
      "rule": "The prefix of a requirement row identifier is the abbreviation of THAT vault's requirements domain, not the constant 'REQ'. homelab writes ANF-BAK-001 in its allocation tables and its rows must be indexed under the same string.",
      "source": "the folder abbreviation whose alias resolves to REQ"
    },
    "unknown_abbreviation": "Reported as export-unknown-domain and excluded from the graph. Never guessed: a silent guess is how an export starts describing a vault it did not understand.",
    "duplicate_role": "One role, one folder. Two folders of one vault that mean the same domain - '01_requirements_(REQ)' beside '01_Anforderungen_(ANF)', or '03_architecture_(ARC)' beside '03_Architektur_(ARC)' - are reported as export-duplicate-role, once per excluded folder, naming both folders, both ingestible file counts and the prefix the graph is written with. The kept one stays the first in sorted order, which is a rule and not a judgement: keeping the fuller folder would move every identifier of the export a second time at whatever unrelated edit tips the count, and ingesting both would write every already-translated requirement into the graph twice and report its untranslated twin as unallocated. Sphinx-Needs resolves a duplicate need ID the same way, first wins plus a warning; Doorstop's silent first-wins on load is filed as a defect there (issue #460). Amendment 2026-08-04f, issue #38. Two folders sharing ONE abbreviation are the same finding with a different closing clause, because Vault keys its index by abbreviation and the last directory readdir returns wins - the export can name that pair but not choose within it. Measured over eleven vault roots: none carries either shape today, so the fixture in tests/run.sh is the only place it is exercised.",
    "enforced": "export-driven"
  },

  "discovery": {
    "path": ".claude/skills/mechatronics-docs/vault_schema.json",
    "rule": "One packaged schema, read from beside validate_vault.py. There is no per-project override; a vault is checked against the schema shipped with the validator that checks it.",
    "rejected_override": [
      "A per-project override at 00_documentation/vault_schema.json was declared by schema 0.1 and is deliberately NOT implemented. It would be a silent off-switch for the blocking gate: the ERRORs that reach the stop gate's blocking set come from check_frontmatter, check_dec_status and check_req_table, which are exactly the checks this file now drives, so a two-line override flipping an entry to 'declared-only' would disable a blocking check from an uncommitted file. That is the same class as 'status: draft' relaxing a rule, which this project rejects outright.",
      "It would also not work where it was meant to. Vault.doc_root is the vault's parent, but a derived project often versions the vault alone - both German production vaults do - so the override path lies outside version control and would not travel with the vault.",
      "A project that genuinely needs different rules forks the skill, which is visible in a diff."
    ],
    "fallback": "If this file is missing or unparseable the validator falls back to a minimal embedded field set and reports schema-unreadable (WARN) once per vault. Validating nothing silently is the failure mode this project has already been bitten by (DECISIONS.md, amendment 2026-07-28).",
    "enforced": "schema-driven"
  },

  "identifier": {
    "field": "id",
    "location": "YAML frontmatter",
    "pattern": "^(REQ|DEC|ARC|CMP|IFC|IMP|TAE|OAU|REF)-[A-Z]{2,4}-[0-9]{3}$",
    "excluded_domains": ["ADM", "INB"],
    "parts": {
      "domain": "folder abbreviation of the object",
      "scope": "subsystem token; the same token the subsystem's REQ file carries in parentheses in its filename",
      "number": "three digits, local to the (domain, scope) pair"
    },
    "rules": [
      "Never reused, gaps allowed - same contract as the REQ row IDs.",
      "REQ files take number 000, reserved for the file itself; REQ table rows use 001 and up, so the file-level and row-level namespaces cannot collide.",
      "Identity lives in the frontmatter, not in the filename: renaming a file does not change its identifier. Wikilinks remain navigation and may be rewritten by the editor.",
      "ADM and INB are excluded: SKILL.md classifies them as not engineering documentation."
    ],
    "assignment": "No registry. The scope token localises the number space; the next free number is found by scanning the vault, following StrictDoc's approach of reading the tree instead of keeping a counter file.",
    "collision_policy": {
      "intent": "Detect, do not prevent. Two branches can assign the same number and merge cleanly.",
      "today": "validate_vault.py reports a collision as id-duplicate (ERROR) and an identifier that was present at git HEAD but is absent from the working tree as id-vanished (WARN). Both are vault-wide checks, so they stay advisory at the stop gate and never block a turn.",
      "enforced": "validator-internal"
    },
    "scope_resolution": {
      "what": "The scope token of a REQ file comes from its own id when one is present, and from the parentheses in its filename otherwise.",
      "consequence": "A REQ file carrying an id survives a rename with the identity of its rows intact. A REQ file without an id keeps the historical filename behaviour, which is what every vault predating this scheme relies on.",
      "residual": "The two sources can disagree. The id wins, and id-scope-mismatch (WARN) reports the divergence at the REQ file rather than leaving it to surface as verifies-unknown-req on an unrelated evidence note.",
      "enforced": "validator-internal"
    },
    "enforced": "declared-only",
    "enforced_detail": "The pattern above is NOT compiled from this file at runtime, and the presence of the field is not required. Two separate reasons. (1) Presence: requiring the field would turn every file of a vault predating this scheme into a finding, and only pattern-conforming values take part in any comparison, so two files freshly copied from one template never collide on its placeholder. (2) The pattern stays a Python constant (ID_RE / REQ_ID_RE / ID_EXCLUDED_DOMAINS) because identity resolution runs in module-level functions without a schema in hand, and because it decides which values are compared against git HEAD - a rule that decides what counts as an identity should not be re-readable from the tree being judged. Drift between the two is caught by tests/run.sh, which asserts that the Python constants and the patterns declared here accept and reject the same samples."
  },

  "code_fences": {
    "note": "Fenced blocks are restricted where the vault holds pointers rather than copies. The rule is about drift against a named source, not about the three backticks: a copy of a file this repository owns drifts against its original, while a block recording the state of, or a command against, a machine this project does not own is not a copy but a record - ISO 9000:2015 3.8.10, 'document stating results achieved or providing evidence of activities performed', which 'need not be under revision control'.",
    "banned_domains": ["IMP", "ARC"],
    "exempt_domains": ["IMP"],
    "exempt_domains_note": "ARC is a map and stores nothing, so no declaration lifts its ban. IMP is where an implementation fact and the observation proving it belong together.",
    "record_max_content_lines": 15,
    "record_max_provenance": "Measured, not chosen: the smallest genuine script excerpt across both German production vaults has 16 content lines, so 15 is the largest value that still catches every measured copy. Effect at that value, same run pair: homelab code-fence 127 -> 9, PMDE 17 -> 7, template vault unchanged.",
    "declaration": {
      "form": "```<language> host=<machine>",
      "position": "any token of the info string, because 23 of 127 blocks in one production vault and 14 of 17 in the other carry no language at all",
      "value": "the name under which the machine is documented - the same name a host-qualified path uses (userver:/etc/...), never an IP, never localhost",
      "spec": "CommonMark 0.31.2: the first word of an info string is typically the language, but 'this spec does not mandate any particular treatment of the info string'. GFM discards further tokens and keeps the language class, so highlighting survives."
    },
    "codes": {
      "code-fence": "ERROR - a block past the threshold with no declaration, and every block in ARC",
      "fence-host": "ERROR - 'host=' naming no machine; a declaration that names nothing is not a declaration",
      "fence-record": "WARN - a declared block past the threshold. Deliberately not silent: nothing can verify that no source file exists, so the exemption stays visible, greppable and countable. It is never an ERROR, because the check cannot distinguish a legitimate record from a disguised copy - Clippy reserves deny-by-default for lints 'free of false positives', Tricorder demands 'no effective false positives' of a blocking check."
    },
    "enforced": "validator-internal",
    "enforced_detail": "The domain lists, the threshold and the attribute name are Python constants (FENCE_BANNED_DOMAINS, FENCE_EXEMPT_DOMAINS, FENCE_RECORD_MAX, FENCE_HOST_RE) for the same reason ID_RE is: they decide what counts as a finding at all, and check_leaks runs per line without a schema in hand. Declared here so the vocabulary has one written home and so a reader finds the reasoning with the rule.",
    "not_enforced": "That a declared block genuinely has no source file. No rule can establish it from the vault alone; it is the doc-claim grep check (follow-up 2 of the 2026-07-25 decision). Validating the machine name against a declared inventory is the other half and has no home yet - there is no per-project registry location, because the per-project schema override was rejected in amendment 2026-07-28d."
  },

  "editor_fields": {
    "note": "Frontmatter keys written by the editor or its plugins. They carry no vault semantics, are never required, and are tolerated in every domain - the undeclared-field check must not report them. Listing them is what keeps that check at WARN-worthy precision instead of flagging a legitimate plugin field as a defect.",
    "values": [
      "tags", "aliases", "cssclasses",
      "publish", "permalink", "description", "image", "cover"
    ],
    "prefixes": ["excalidraw-"],
    "provenance": "Obsidian Help, 'Properties': tags/aliases/cssclasses are the default properties, publish/permalink/description/image/cover the Obsidian Publish ones. The deprecated singular spellings tag/alias/cssclass are deliberately absent - support for them was dropped in Obsidian 1.9, so a file still carrying one should be reported. A prefix covers the Excalidraw plugin, which writes a whole excalidraw-* family that no enumeration would stay current with.",
    "enforced": "schema-driven"
  },

  "domain_defaults": {
    "note": "The vault-wide field vocabulary. It applies to EVERY domain, including the ones 'domains' below does not name - a vault in another language spells its folders ANF/ENT/KMP/SST/TUE/BUN, and reading only the named domains would leave those files unchecked. Per-domain entries in 'domains' merge into this profile attribute by attribute, so a domain that only changes 'required' keeps the type, the values and the finding code declared here. The vault's own SSOT rule applied to its schema: a field shared by every domain is declared once.",
    "field_keys": {
      "type": "folder-abbreviation | enum | date | list | identifier",
      "values": "permitted values, for type enum",
      "format": "for type date",
      "item": "element type, for type list",
      "required": "whether a missing key is a finding",
      "code": "finding code emitted when the value violates its type",
      "empty_code": "finding code (WARN) when a required list is empty",
      "hint": "appended to the missing-key message",
      "enforced": "see enforcement_levels; anything but schema-driven is not checked"
    },
    "fields": {
      "domain": {
        "type": "folder-abbreviation",
        "required": true,
        "code": "frontmatter-domain",
        "note": "The value must equal the abbreviation of the folder the file sits in. Declared as a type rather than an enum per domain, because the same rule has to hold for a domain this schema does not name.",
        "enforced": "schema-driven"
      },
      "status": {
        "type": "enum",
        "values": ["draft", "active", "superseded", "deprecated"],
        "required": true,
        "code": "frontmatter-status",
        "note": "status: draft relaxes no other check.",
        "enforced": "schema-driven"
      },
      "created": {
        "type": "date",
        "format": "YYYY-MM-DD",
        "required": true,
        "code": "frontmatter-date",
        "enforced": "schema-driven"
      },
      "last-verified": {
        "type": "date",
        "format": "YYYY-MM-DD",
        "required": true,
        "code": "frontmatter-date",
        "enforced": "schema-driven"
      },
      "id": {
        "type": "identifier",
        "required": false,
        "note": "Declared vault-wide so that an id is never reported as an undeclared field. The value itself is not checked - see identifier.enforced_detail.",
        "enforced": "declared-only"
      },
      "verifies": {
        "type": "list",
        "item": "req-row-identifier",
        "required": false,
        "code": "verifies-format",
        "empty_code": "verifies-empty",
        "note": "Declared vault-wide, enforced only in TAE (see domains.TAE). A vault in another language carries this field on its own evidence domain - homelab has 37 such files, all with an empty list - and enforcing the rule there would be a convention rollout on a vault that never had the convention, not a defect report. Extending it belongs to the domain-alias follow-up of amendment 2026-07-28.",
        "enforced": "declared-only"
      }
    }
  },

  "domains": {
    "REQ": {
      "note": "Carries the default fields. The identified object in REQ is the table row, not the file; the file's own id reserves number 000.",
      "fields": {
        "id": {"note": "REQ-<SCOPE>-000"}
      },
      "rows": {
        "id_pattern": "^REQ-[A-Z]{2,4}-[0-9]{3}$",
        "columns": ["class", "number", "content", "acceptance_criterion", "source"],
        "class_values": ["M", "S", "O"],
        "recognition": "A requirement table is recognised by its shape, never by the wording of its header (issue #25, amendment 2026-08-04b): a GFM table - a delimiter row with a header row of the same width directly above it - of at least five columns, whose header carries the template's tokens OR whose rows carry a three-digit number in the second cell, which is the predicate the requirement index uses to decide that a row defines a requirement. A header row is not enforced anywhere and does not survive translation, so keying on its text let one quoted example switch four blocking checks off. The section-title binding table_bindings uses for the exporter was measured and rejected for this: it would leave 34 of the 121 requirement rows on this machine unchecked.",
        "unrecognized": "req-table-unrecognized (WARN, one grouped finding per file) reports the rows no row check reads: a table the requirement index takes rows from while check_req_table does not, or a REQ file carrying a table wide enough to be a requirement table and not one readable table. WARN and not ERROR because the check cannot tell a revision log or a rubric - both legitimate in a REQ file - from a table that drifted out of readability.",
        "enforced": "validator-internal",
        "enforced_detail": "class_values IS read from here (req-class). The rest - req-nnn, req-duplicate, req-criterion, req-duplicate-global, req-table-unrecognized - stays in Python because it is table structure rather than a value vocabulary. The column count in particular: as data it would be an uncommitted off-switch for four blocking codes, which is what discovery.rejected_override refuses. id_pattern is compared against the validator's REQ_ID_RE by the test suite instead of being compiled at runtime."
      }
    },
    "DEC": {
      "note": "The only domain without a frontmatter status: a decision carries its state in the body Status line instead, so that the state and its justification stay in one place.",
      "fields": {
        "status": {
          "required": false,
          "note": "Not required, but checked against the same value list when present, so a stray frontmatter status cannot contradict the body Status line unnoticed."
        }
      },
      "body_fields": {
        "Status": {
          "type": "enum",
          "values": ["Draft", "Accepted", "Superseded", "Deprecated"],
          "required": true,
          "code": "dec-status",
          "location": "body line 'Status: <value>'",
          "enforced": "schema-driven",
          "enforced_detail": "The value list is read from here. The companion rule - Status 'Superseded' requires a 'Superseded by: [[DEC_...]]' link (dec-superseded, ERROR) - stays in Python: it is a cross-reference requirement, not a value vocabulary. See relations/superseded-by."
        }
      }
    },
    "ARC": {"note": "Carries the default fields.", "fields": {}},
    "CMP": {"note": "Carries the default fields.", "fields": {}},
    "IFC": {"note": "Carries the default fields.", "fields": {}},
    "IMP": {"note": "Carries the default fields.", "fields": {}},
    "TAE": {
      "note": "The evidence domain. It is the only domain in which 'verifies' is required and checked.",
      "fields": {
        "verifies": {
          "required": true,
          "hint": "verifies: [REQ-...]",
          "enforced": "schema-driven"
        }
      }
    },
    "OAU": {"note": "Carries the default fields.", "fields": {}},
    "REF": {"note": "Carries the default fields.", "fields": {}}
  },

  "link_annotation": {
    "form": "[[<exact filename>]] (<identifier>)",
    "rule": "Every cross-domain link in a domain file is annotated with the target's identifier. Because an identifier begins with its domain token, an annotated link carries its own type - no heading is needed to interpret it.",
    "example": "[[CMP_Battery_Pack]] (CMP-BAT-001)",
    "enforced": "declared-only"
  },

  "table_bindings": {
    "note": "A relation table is recognised by the SECTION it sits in, and the section title comes from the project's own 00_*template* file. Files whose name starts with 00_ are excluded from ingestion: templates and READMEs carry the same tables with illustrative rows pointing at objects that do not exist. They are read for one purpose only - to learn what this project's sections are called.",
    "binding_discovery": {
      "step_1": "Read the domain's own template files. For every H2 section that contains a table, record the section title and the table's header cells.",
      "step_2": "Classify each template table by column count, and for the two four-column ARC tables by whether the last header cell case-folds to the status token: that cell is the one word which is spelled identically in every corpus measured (English 'Status', German 'Status'). The other four-column table is the interface table.",
      "step_3": "In a real file, find the section whose title matches the template's, comparing with the same fold-insensitive rule check_sections uses, and take the first table inside it. Column roles are positional.",
      "why_not_the_header": "Nothing enforces a header row, and the drift is already in the corpus: homelab's ARC template writes 'Verifikation (TUE)' while six of its fourteen ARC files write 'Verifikation (TST)'. A recognizer keyed on the header accepts 8 of 14 files and 73 of 148 allocation rows. The section title is enforced - check_sections reports a missing or retitled section as a blocking ERROR - and is present in 14 of 14. Measured 2026-07-31.",
      "header_signature_role": "The declared header_signature below is no longer the binding. It is kept as corroboration: a bound table whose header does not match is still ingested, and the divergence is reported as export-header-drift so the vault can be tidied without any row being lost meanwhile.",
      "unbound_table": "Reported as export-unbound-table, and asked differently in the two domains that carry a binding, because they lose rows differently. ARC: a table that sits in no bound section. REQ: a requirement row that did not reach the graph, counted per table and anchored at the first such row. Measured across the nine vault roots on this machine, the exporter drops 112 requirement rows and 78 of them sit INSIDE the bound section, in its second to tenth table - bound_tables reads the first table of the section and sections_with_tables sees no other, so no section-title check can name them. The same check would also state something false: 'sits in no declared section' is wrong whenever an incidental table merely stands in front of the requirement table in a section that IS declared, and the bound REQ section is the prose section ('## Context', '## Kontext') in all nine vaults. Amendment 2026-08-04d, issue #34.",
    "unbound_table_scope": "The seven domains for which this project declares no table binding are exempt, and the exemption is measured rather than assumed: reporting every table they carry adds 663 findings across the same nine roots, including one in the shipped template vault, which is a convention rollout and not a defect report. It is the shape amendment 2026-08-04b rejected as option E for the validator, and the argument carries here even though an exporter finding never blocks a turn: a report nobody can act on trains its reader to skip the section. An empty graph is never silent about a row it could have carried."
    },
    "status_token": "status",
    "exclude_filename_prefix": "00_",
    "arc_allocation_table": {
      "domain": "ARC",
      "header_signature": ["Submodule (ARC/CMP/IFC)", "Allocated Requirements (REQ-IDs)", "Verification (TAE)", "Status"],
      "column_roles": ["subject", "objects:allocates", "objects:evidence", "qualifier:status"],
      "qualifier_values": ["Draft", "Approved", "Verified", "Deprecated", "Blocked"],
      "qualifier_proven_value": "Verified",
      "qualifier_proven_note": "The one value that lets a requirement count as proven. Named here rather than inferred, because 'the last value in the list' and 'the longest one' are both accidents of how the list was written. The German vaults write these five values in English too, so the vocabulary needs no alias."
    },
    "arc_interface_table": {
      "domain": "ARC",
      "header_signature": ["Interface (IFC)", "Endpoint A", "Endpoint B", "Context"],
      "column_roles": ["subject", "object:connects", "object:connects", "note"]
    },
    "arc_main_module_table": {
      "domain": "ARC",
      "columns": 2,
      "header_signature": ["Submodule (ARC)", "Brief Description"],
      "column_roles": ["object:contains", "note"],
      "note": "The main-module template's submodule table. It is where an ARC-to-ARC edge is authored, and without it a requirement chain cannot roll up to its top-level module - PMDE nests 26 ARC files five levels deep this way."
    },
    "req_table": {
      "domain": "REQ",
      "columns": 5,
      "header_signature": ["Class (M/S/O)", "NNN", "Content", "Acceptance Criterion", "Source / Justification (REF/DEC)"],
      "column_roles": ["qualifier:class", "subject", "text", "text", "objects:justified-by"],
      "note": "Links are read from the justification column alone, never from the row. Two homelab requirement rows carry a wikilink inside the acceptance criterion, and a row-wide scan would turn those into justified-by edges that nobody authored."
    },
    "enforced": "export-driven",
    "enforced_detail": "export_traceability.py reads these bindings; validate_vault.py still does not, and no rule asserts that a vault's own tables match the declared header signatures. That asymmetry is deliberate: the exporter produces a report, so a mis-bound table costs a finding in a document, whereas the same rule in the validator would block a turn on formatting drift the author may not own. Residual 4 of amendment 2026-07-28b - reformatting a header row silently removes every relation it carried - is closed by the section binding rather than by a validator rule."
  },

  "relations": {
    "note": "Each kind is authored in exactly one place and in exactly one direction. The reverse direction is computed, never authored - the pattern StrictDoc, Doorstop and Sphinx-Needs all use. Reverse keys follow the Sphinx-Needs '<kind>_back' convention, except where the reverse already has a natural name.",
    "allocates": {
      "subject_domains": ["ARC", "CMP", "IFC"],
      "object_domains": ["REQ"],
      "authored_in": "arc_allocation_table",
      "reverse_key": "allocates_back",
      "outgoing": "allocates",
      "incoming": "is allocated to",
      "enforced": "export-driven"
    },
    "evidence": {
      "subject_domains": ["ARC", "CMP", "IFC"],
      "object_domains": ["TAE"],
      "authored_in": "arc_allocation_table",
      "qualifier": "status",
      "reverse_key": "evidence_back",
      "outgoing": "is verified by",
      "incoming": "verifies the allocation of",
      "semantics": "A row with several requirements and several evidence notes states that the listed notes together cover the allocation. Which note proves which requirement is answered by 'verifies', not by this relation.",
      "qualifier_matching": "A status counts as the declared value only on an exact match. A cell that begins with a declared value and carries more text - homelab has 19 such rows across 17 distinct spellings, 'Verified (Rebuild: Draft)' among them - is reported as qualified, is NOT counted as proven, and its verbatim text becomes the close-out reason. Neither equality nor prefix matching alone is honest here: equality calls 'Verified (Rebuild: Draft)' unproven without saying why, and prefix matching calls 'Verified (bis 2026-07-25 verletzt)' proven.",
      "enforced": "export-driven"
    },
    "verifies": {
      "subject_domains": ["TAE"],
      "object_domains": ["REQ"],
      "authored_in": "TAE frontmatter field 'verifies'",
      "reverse_key": "verifies_back",
      "outgoing": "verifies",
      "incoming": "is verified by",
      "enforced": "validator-internal",
      "enforced_detail": "The field's presence and the format of its elements are schema-driven (domains.TAE). That the referenced REQ rows exist is checked in Python (verifies-unknown-req), because it needs the vault-wide REQ row index rather than a value vocabulary."
    },
    "justified-by": {
      "subject_domains": ["REQ"],
      "object_domains": ["DEC", "REF"],
      "authored_in": "req_table",
      "reverse_key": "justified-by_back",
      "outgoing": "is justified by",
      "incoming": "justifies",
      "note": "Issue #4 calls this kind 'references'. Named justified-by here after the REQ table column it is authored in ('Source / Justification'), because a bare 'references' would name the mechanism rather than the meaning - every relation in this file is a reference.",
      "enforced": "export-driven"
    },
    "connects": {
      "subject_domains": ["IFC"],
      "object_domains": ["CMP", "ARC"],
      "authored_in": "arc_interface_table",
      "cardinality": "exactly two objects per row",
      "reverse_key": "connects_back",
      "outgoing": "connects",
      "incoming": "is connected by",
      "enforced": "export-driven"
    },
    "contains": {
      "subject_domains": ["ARC"],
      "object_domains": ["REQ", "DEC", "CMP", "IMP"],
      "authored_in": "annotated links in the ARC body lists",
      "subtype_from": "domain token of the object identifier",
      "reverse_key": "contains_back",
      "outgoing": "contains",
      "incoming": "belongs to",
      "authored_in_secondary": "arc_main_module_table",
      "enforced": "export-driven"
    },
    "test-object": {
      "subject_domains": ["TAE"],
      "object_domains": ["CMP", "IFC", "ARC"],
      "authored_in": "annotated link in the TAE Test Conditions section",
      "reverse_key": "test-object_back",
      "outgoing": "was tested on",
      "incoming": "was the test object of",
      "enforced": "export-driven"
    },
    "superseded-by": {
      "subject_domains": ["DEC"],
      "object_domains": ["DEC"],
      "authored_in": "DEC body line 'Superseded by: [[DEC_...]]'",
      "reverse_key": "supersedes",
      "outgoing": "is superseded by",
      "incoming": "supersedes",
      "note": "Issue #4 names this kind 'supersedes'. It is authored in the superseded decision, so the subject of the authored line is the OLD decision and the authored direction is superseded-by; 'supersedes' is the computed reverse. Naming it the other way round would reverse the arrow against the other seven kinds, which are all authored subject-first.",
      "enforced": "validator-internal",
      "enforced_detail": "dec-superseded (ERROR) requires the link to be present whenever the body Status is 'Superseded'. The link target is not resolved to a DEC object; that is graph work and belongs to issue #2."
    }
  },

  "consistency_rules": [
    {
      "id": "evidence-covers-allocation",
      "statement": "For every allocation row, the requirements in the 'verifies' frontmatter of the referenced evidence notes should together cover the requirements allocated in that row.",
      "why": "'verifies' and 'evidence' are separate relations with separate subjects and can disagree without either being wrong on its own.",
      "enforced": "export-driven",
      "owed_to_issue": 2,
      "note": "Reported by the exporter as evidence-covers-allocation, never by the validator. The rule compares two relations, and neither is extracted until something reads the allocation table into a graph."
    },
    {
      "id": "verified-needs-evidence",
      "statement": "An allocation row with status 'Verified' must carry at least one evidence link.",
      "enforced": "export-driven",
      "owed_to_issue": 2,
      "note": "Reported by the exporter as an informational class, deliberately not as a defect. Measured 2026-07-31 across all six production vaults: 21 findings on homelab and zero everywhere else, and all 21 are rows whose verification is documented as prose ('Preflight in jedem Lauf', 'Vollstaendiger Backup-Lauf 2026-03-23') rather than as a link. Prose verification is thin, but it is not the same defect as an unverified allocation claiming to be verified, and a check that cannot tell them apart is the false-positive class this project refuses in anything that blocks."
    },
    {
      "id": "identifier-uniqueness",
      "statement": "An identifier appears at most once across the vault, and an identifier present in an earlier commit does not silently disappear.",
      "enforced": "validator-internal",
      "enforced_detail": "id-duplicate (ERROR) for the first clause, id-vanished (WARN) for the second; ADM and INB are excluded from both. The severities differ because the checks differ in kind: a collision is never legitimate, whereas a disappearance can equally be a retirement, a rename or an accidental loss, and no validator can tell those apart."
    },
    {
      "id": "declared-fields-only",
      "statement": "A frontmatter key is either declared for its domain, or declared as an editor field, or it is reported.",
      "why": "A mistyped key is the one defect class that fails completely silently: the field looks present and takes effect nowhere.",
      "enforced": "schema-driven",
      "enforced_detail": "frontmatter-undeclared (WARN), one grouped finding per file listing the unknown keys. WARN and not ERROR because the check cannot distinguish a typo from a deliberate plugin field - Clippy reserves its deny-by-default correctness group for lints free of false positives and files anything that might be intentional under warn-level suspicious, and Tricorder requires a blocking check to produce no effective false positives. Measured cost when introduced: zero findings on the template vault and on both German production vaults."
    }
  ],

  "untyped_links": {
    "note": "Links that remain navigation in the worked example and are deliberately not relation edges. Listed so the gap is visible rather than implied.",
    "items": [
      "Links from a domain file to a 00_ README or template (documentation pointers).",
      "Links inside prose in a Context section that repeat a relation already authored elsewhere in the same file."
    ]
  }
}
